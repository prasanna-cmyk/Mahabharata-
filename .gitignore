# Media: track the persistent, regenerable-source deliverables in git so they
# survive sandbox restarts; keep heavy intermediates and video local + in the
# Drive backup ZIP.
Media/*
!Media/build_pilot.sh
!Media/Keyframes4K/
!Media/Keyframes4K/*
!Media/Audio/
!Media/Audio/*.mp3
